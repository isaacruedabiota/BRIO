# BRIO — Mobile client

Flutter client for BRIO, an all-in-one fitness app (nutrition + training + cardio).

See the [repository README](../README.md) for the full project overview, and
`lib/core/config/app_config.dart` for backend URL configuration.

## Run

```bash
flutter pub get
flutter run
```
